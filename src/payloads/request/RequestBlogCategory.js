export default class RequestBlogCategory {
    constructor(id,category_name,category_slug,descriptions) {
        this.id = id;
        this.category_name = category_name;
        this.category_slug = category_slug;
        this.descriptions = descriptions;
    }
}
