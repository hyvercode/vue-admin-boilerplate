export default class RequestBlogTag {
    constructor(id,tag_name,tag_slug,descriptions) {
        this.id = id;
        this.tag_name = tag_name;
        this.tag_slug = tag_slug;
        this.descriptions = descriptions;
    }
}
