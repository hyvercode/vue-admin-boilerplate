export default class RequestEticketCategories {
    constructor(id,
                category_name,
                active,) {
        this.id = id;
        this.category_name = category_name;
        this.active = active;
    }

}
