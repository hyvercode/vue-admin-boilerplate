export default class RequestEticketMilestones {
    constructor(id,
                milestone_name,
                active,) {
        this.id = id;
        this.milestone_name = milestone_name;
        this.active = active;
    }

}
