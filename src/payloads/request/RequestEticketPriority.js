export default class RequestEticketPriority {
    constructor(id,
                priority_name,
                active,) {
        this.id = id;
        this.priority_name = priority_name;
        this.active = active;
    }

}
