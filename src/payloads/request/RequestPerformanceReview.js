export default class RequestPerformanceReview {
    constructor(id,
                self_achievement,
                self_evaluation,
                self_kpi_point,
                self_notes,
                status,) {
        this.id = id;
        this.self_achievement = self_achievement;
        this.self_evaluation = self_evaluation;
        this.self_kpi_point = self_kpi_point;
        this.self_notes = self_notes;
        this.status = status;
    }
}
