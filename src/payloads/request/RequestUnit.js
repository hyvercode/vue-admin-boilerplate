export default class RequestUnit {
    constructor(id,
                active,
                unit_name,
                head_id) {
        this.id = id;
        this.active = active;
        this.unit_name = unit_name;
        this.head_id = head_id;
    }

}
